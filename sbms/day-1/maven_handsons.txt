1. create a standlone maven app - create a java class (Welcome) - try maven goal (clean, compile, package). 
   - need of making java version <maven.compiler.target>
   - role of <finalName> in pom.xml

2) create a maven web application - understand the folder structure of web application
	- correct the errors in pom.xml
        - make src/main/java & src/test/java available in package explorer of IDE

3) Create a project to understand "maven install" goal

4) use the created dependency in you newly created maven project

5) create an executable jar file for the first project

6) create an executable jar file for the forth project (

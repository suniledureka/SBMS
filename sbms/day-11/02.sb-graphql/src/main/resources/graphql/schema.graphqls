type Movie {
  id: ID!
  title: String!
  year: Int!
  genres: [String!]!
  director: String!
}

type Query {
  movieById(id: Int!): Movie
  allMovies: [Movie!]!
}

type Mutation{
    addMovie(id: ID!
    title: String!	
    year: Int!
    genres: [String]!
    director: String!): Movie
}
